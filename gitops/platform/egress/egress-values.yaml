# Auto-generated template: populate from Terraform outputs and SOPS-encrypt before committing.
# Do not store plaintext secrets in this file.

egress:
  ips:
    - "203.0.113.10"   # replace with terraform output egress_ips[0]
    - "203.0.113.11"   # replace with terraform output egress_ips[1]

loadBalancer:
  ip: "198.51.100.5"   # replace with terraform output lb_ip
  hostname: "api.example.com"  # optional

backups:
  bucket: "my-org-backups-prod"  # replace with terraform output backup_bucket
  region: "us-east-1"

artifacts:
  registry:
    host: "ghcr.io"    # container registry host used by CI
    repo: "yourorg"   # registry repo prefix

metadata:
  generated_by: "terraform-ci"
  generated_at: "2026-02-23T00:00:00Z"  # CI should set timestamp
